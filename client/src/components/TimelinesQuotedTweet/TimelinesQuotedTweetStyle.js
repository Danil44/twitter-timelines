import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  card: {
    maxWidth: '100%',
  },

  name: {
    marginRight: 5,
  },

  count: {
    marginRight: 15,
  },
});

export default useStyles;
